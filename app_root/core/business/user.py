from app_root.core.data_model.auth import User

def register_user(user_registration):
    new_user = User()

    return

def authenticate_user(username, password):

    return True


def _get_password_hash(password):
    return

def _check_password(password, user):
    return
